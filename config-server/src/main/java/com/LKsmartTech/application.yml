server:
  port: 8888
spring:
  application:
    name: config-server
    cloud:
      config:
        server:
          git:
            uri: https://github.com/LucasAKuhn/microsservicos

            default-label: main
            search-paths:
              - 'greeting-service*'